## CHANGES TO LAB:


## Current state - current optoins:
1. Daily:
    - Single outcome with tags
    - Single outcome with no tags
2. Event
    - Log no severity
    - Log with severity

## Desired state:

1. Daily:
    - Possibility to add multiple outcomes (same with scale) that would be either with or without tags. If with tags, then in the LAB section we'd need to add a dropdown menu, where user can see for wich outcome he's looking for correlations. 
2. Event stays as it is.
3. Addition - just tags, no scale. For daily log, logged once a day. In Labs the only the tag frequency could be shown. Ok, additional feature - when tag is clicked, also this 30 day table is shown (similar as with multiple choice questions). User can select up to 5 tags, to see them in this table. 
4. Addition - multiple choice (when set up, user can chose either only 1 can be selected, or multiple). User defined them when making the project. Ir LABS page the only visual results the usershould see would be a table, showing 30 days. Days would be on X axis and answers would be on Y. And results would be just shown with green glowing dot in the table. The table should show 30 days by default. User can chose the starting date for these 30 days. Once page is closed, starting date returns to default (which in case of data of more then 30 days would be so, that the current date is last. If less then 30 days logged, then also, current date is last, all previous just empty, but still shown. )



Final project types:
- Daily - Track one outcome per day -> rename -> "Daily - Track outcome"
- Daily - Tag only
- Daily - Multiple-choice
- Event - Log sporadic occurrences


- Generally for tags - Would be nice to add custom categories for tags, so when logging them daily it would be for user to oversee the tags. User can define the categories when making the project, and also add them later on. 

